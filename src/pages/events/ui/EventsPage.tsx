import { PlaceholderPage } from '@/shared/ui'

export function EventsPage() {
  return (
    <PlaceholderPage
      label="Operations"
      title="Events"
      description="Event scheduling lives here. Requires the editor role or above — sign in as viewer@vfl.com to see the guard redirect to /forbidden."
    />
  )
}
