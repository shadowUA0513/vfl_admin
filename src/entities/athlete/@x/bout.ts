/* Cross-import API for the `bout` entity: a bout embeds both corners and,
   once a result is recorded, the winner. */
export type { Athlete } from '../model/types'
export { athleteName } from '../model/types'
